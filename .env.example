# KitStack.org — no environment variables required for the free static tools.
# Core tools run entirely in the browser (no backend, no API keys).

# Optional: public site URL if you want self-referential links in a custom deploy.
# APP_URL=https://kitstack.org
